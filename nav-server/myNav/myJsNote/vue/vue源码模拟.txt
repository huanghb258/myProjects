数据绑定{{}}大致思路：
	1.遍历#app下的所有元素节点下的文本节点，用递归（div里面还有div）
	2.用正则匹配{{name}}:exp=/\{\{\(.*)}\}/	
	3.获取到{}里的内容name,即exp.$1
	4.



事件指令：
	1.从指令名中取出事件名
	2.根据指令的值（表达式）从methods中得到对应的事件处理函数对象
	3.给当前元素节点绑定指定事件名和回调函数的dom事件监听
	4.指令解析完后，移除指令属性


双向数据绑定：
	
	
	